'use strict';

(function () {

  /**
  * Represents copyright medhods.
  */

  /** Main object. */
  var translator = {
    /** Get dom element. */
    copyrightDom: $('.copyright'),
    /** Get current Year */
    y: new Date().getFullYear(),
    /** Init main method. */
    init: function () {
      
    }
  };

  translator.init();

} ());
